use std::io::{self, Write};

fn main() {
    // Arrays and data types
    // let a = [1,2,3,4,5];

    // print!("Please enter array index: ");
    // io::stdout().flush().unwrap();

    // let mut index = String::new();

    // io::stdin()
    //     .read_line(&mut index)
    //     .expect("Failed to read line");
    
    // let index: usize = index
    //     .trim().parse().expect("Index not a number");
    
    // let element = a[index];

    // println!("The value of index element at {index} is {element}.");
}
